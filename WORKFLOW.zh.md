# 从一条抖音视频到一篇成文的整理

> 一个真实感的人跑完 Kei-Second-Brain 第一个完整闭环的故事——所有 skill 各出场一次。**人物是虚构的**；故事里触碰的示例文件是**本仓库真实存在的文件**——边读故事边打开它们对照。

故事发生在 **2026-09-15** 所在的一周。`.agents/skills/` 里的每个 skill 恰好出现一次。

---

## 主角

**Alex**，最近从零开始学电脑硬件。没装过机，只是好奇机器怎么工作，还攒了一堆从来没串起来的笔记。这周，Alex 找到了一套真正能闭环的方法。

---

## 第一步 · 采集——一条视频变成一条剪藏

Alex 刷抖音刷到**小坚果搞机**（DIY 电脑装机博主）讲**显示器怎么选**——尺寸、分辨率、刷新率。视频有用，于是按系统的预期动作走：

1. 把视频链接丢进**链接转文字工具**。
2. 拿到逐字稿。
3. 把逐字稿**按原文、不改动**剪进知识库，作为原始剪藏：

```
0_Inbox/clippings/xiaojian-guo-monitor-episode.md
```

> 📄 **真实文件——打开它。** 它是整个系统的输入素材：原始、真实、还没被归类为任何知识。注意它**没有**什么：没有结构、没有类型、没有标签。这就是收件箱的意义——只负责收集。

**采集原则**：剪藏是输入素材。采集时永远不改写；"这是什么知识"由编译环节决定。

---

## 第二步 · 编译——素材变成知识

收件箱里有料了，Alex 对它跑编译 skill：

```
/by-1   →   "编译这条剪藏"
```

agent 读完剪藏，判定它是**框架（framework）**——它给出了选显示器的步骤/判断标准——然后写成一条类型化条目：

```
1_Wiki/frameworks/monitor-selection-guide.md
```

> 📄 **真实文件——打开它。** 注意编译加进了什么：
> - **Frontmatter** —— `type: framework`、`source: 0_Inbox/clippings/xiaojian-guo-monitor-episode.md`（可溯源）、`confidence: high`、`author: agent`。
> - **结构化正文** —— 三个决策轴做成表格（尺寸 / 分辨率 / 刷新率）。
> - **诚实的过滤** —— 视频里的引流话术（"私信我免费帮你选"）被**丢弃**了：它不是知识。
> - **`## See also`** —— 故意留空：还没有可说明的关系，链接靠挣不靠分配。

之后几天 Alex 继续刷（CPU、显卡、内存），每期都剪藏，然后跑批量版：

```
/by-a   →   "编译全部"
```

它扫描收件箱、把未编译的剪藏全部编译、回写 `processed: true`，并产出编译报告：

```
3_KBI-log/2026-09-15-compile-report.md
```

> *（示例库每类只保留一条，所以 CPU/显卡/内存那些条目是示意——你的库里会长出真的来。）*

**编译原则**：素材只有在这里才变成知识——带类型、带来源、带置信度、链接靠挣。

---

## 第三步 · 沉淀与记录——对话和日记

知识不全来自视频。Alex 问 AI"2K 显示器配什么显卡够用"，对话里有一条真正可复用的结论。这正是 `/chat-distill` 的用途——按价值筛选 AI 对话，只留值得留的：

```
/chat-distill   →   "沉淀这周的对话"
```

入选的精华片段变成带对话专属 frontmatter 的原始笔记（`source_type: chat`、`source_agent: agent`），再像其他素材一样被编译进 wiki：

```
0_Inbox/chat-distill/2026-09-15-agent-example.md
```

每天晚上，Alex 用 `/rec-1` 把当天进展记进日记（只追加、一行一件事）：

```
/rec-1   →   "记一下今天的进展"
0_Inbox/diary/2026-09-15.md
```

**记录原则**：日记也是原始素材——只追加，不改写。

---

## 第四步 · 迭代——让知识库保持诚实

这时 wiki 已经有十几条了。Alex 跑健康检查：

```
/kbi   →   "知识库迭代"
```

agent 扫遍全库，产出一份**只读**的迭代报告：

```
3_KBI-log/2026-09-15-iteration-report.md
```

发现了什么：

| 发现 | Alex 的处理 |
|------|------------|
| 旧条目里一条断链 | 修正指针 |
| 一条孤岛条目、没有真实关联 | 放着不动——孤岛有时是有意独立 |
| 知识缺口：没有"显卡天梯"条目 | 问 AI 显卡天梯相关问题，然后通过 `/chat-distill` 补上 |
| 一条疑似幻觉内容 | 删掉（规则：AI 提议未验证的必须标注 `> 💡 AI proposal (unverified)`） |

Alex 还跑了主题聚合，把整个硬件主题收成一张图：

```
/jh-1   →   "聚合电脑硬件主题"
```

它产出知识地图（哪些条目是父子/并列、哪里缺链接）——用主题视角决定接下来学什么。

**迭代原则**：知识库永远没有"完成"——健康检查和主题地图，是编译出的知识保持可信的手段。

---

## 第五步 · 规划与复盘——方向和反思

学习需要计划，Alex 用 `/chiselplan` 开了一个项目：

```
/chiselplan   →   "规划我的电脑学习项目"
```

它写下一份计划文件：最终目标 + 任务阶段（地基→跑通→深化）。运行中它还能创建一个配套的"每日循环"skill（默认名 `plan-1`），每天从当前阶段滚出 3-5 条任务——这周 Alex 没开，保持简单。

周末，`/wr-1` 把七天日记编译成周复盘：

```
/wr-1   →   "每周复盘"
0_Inbox/weekly-review/0915-last-week-review.md
```

**规划与复盘原则**：素材向前喂（计划）、向后照（复盘）——循环自我纠偏。

---

## 第六步 · 输出——知识变成内容

最后，Alex 想把学到的东西写出来。`/blog-1` 把编译条目变成文章：

```
/blog-1   →   "文章：电脑硬件整理，取材我的 wiki 条目"
```

它生成带 frontmatter + slug 的 markdown 文件，放进输出层：

```
4_Outputs/pc-hardware-notes.md
```

> 文章是**从编译条目写出来的**，不是凭记忆——来源和置信度跟着走。Alex 把它发到了个人博客。有个读者问了句"刷新率到底多少够用"，这个问题成了 Alex 看的下一期视频、下一条剪藏、下一次编译。

**输出原则**：永远不离开库的知识只是日记。输出闭环——而闭环会自己喂自己。

---

## 整个循环，一张图

```
       采集                 编译                  维护                  输出
视频/逐字稿 ──▶ 0_Inbox ──▶ 1_Wiki ──▶ kbi / jh-1 检查 ──▶ 4_Outputs ──▶ 文章
     ▲                  │            │                │                   │
     │                  │            ▼                ▼                   │
     └──── 新问题 ◀── 日记 / 周复盘 ◀── 计划 / 对话沉淀 ◀──────────────────┘
```

素材流进来；知识被编译、被维护、再作为内容推出去；内容带来新问题；新问题变成新素材。

---

## Skill 地图——9 个，各自在什么时候跑

| Skill | 触发词 | 在这个故事里 |
|-------|--------|--------------|
| `/by-1` | "编译这条" | 第二步——显示器剪藏 → 一条框架条目 |
| `/by-a` | "编译全部" | 第二步——批量编译 CPU/显卡/内存剪藏 |
| `/chat-distill` | "沉淀对话" | 第三步——留下一条有价值的 AI 对话 |
| `/rec-1` | "记一下" | 第三步——往日记追加一行 |
| `/kbi` | "知识库迭代" | 第四步——健康检查：断链、缺口、幻觉 |
| `/jh-1` | "聚合主题" | 第四步——电脑硬件知识地图 |
| `/chiselplan` | "规划项目" | 第五步——学习项目分阶段（可创建 `plan-1`） |
| `/wr-1` | "每周复盘" | 第五步——7 篇日记 → 周复盘 |
| `/blog-1` | "文章/项目/分享" | 第六步——电脑硬件整理 → `4_Outputs/` |

一开始不需要九个全会。最小起步是 **采集 → `/by-1` → 读条目**。等循环需要了，再加其他的。
